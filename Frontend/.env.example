# Frontend environment variables
# Copy to `.env` (local) or set in your hosting provider.

# API base URL MUST include `/api/v1`
# Local example: http://localhost:2804/api/v1
VITE_BASE_URL=https://YOUR_BACKEND_DOMAIN/api/v1

# ImageKit public URL endpoint (used for static assets/videos)
VITE_IMAGEKIT_URL=https://ik.imagekit.io/YOUR_IMAGEKIT_ID

# Google OAuth entrypoints
# Alumni is implemented on backend: /api/v1/alumni/google
VITE_GOOGLE_URL1=https://YOUR_BACKEND_DOMAIN/api/v1/alumni/google
# Student Google login is not implemented (leave blank unless you add it)
VITE_GOOGLE_URL2=
